server:
  port: 8084

spring:
  application:
    name: enrollment-service

  datasource:
    url: jdbc:mysql://localhost:3306/learnova_enrollment_db
    username: root
    password: Bharat_007
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    enabled: false
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html